name: Format challenges feature

on:
  push:
    branches:
      - feature/challenges-domain-cycle-1
    paths:
      - 'platform/apps/admin-web/src/ChallengesPanel.tsx'
      - 'platform/apps/admin-web/src/main.tsx'
      - 'platform/apps/admin-web/src/styles.css'
      - '.github/workflows/format-challenges-feature.yml'

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: platform
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/challenges-domain-cycle-1
      - uses: pnpm/action-setup@v4
        with:
          version: 10.15.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: platform/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec prettier --write apps/admin-web/src/ChallengesPanel.tsx apps/admin-web/src/main.tsx apps/admin-web/src/styles.css
      - name: Commit formatting
        working-directory: .
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add platform/apps/admin-web/src/ChallengesPanel.tsx platform/apps/admin-web/src/main.tsx platform/apps/admin-web/src/styles.css
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "style: aplica prettier no módulo de desafios"
          git push origin HEAD:feature/challenges-domain-cycle-1
