name: Integrate operations panel

on:
  push:
    branches: [feature/operations-overview-cycle-1]
    paths: [.github/workflows/operations-panel-integrate.yml]

permissions:
  contents: write

jobs:
  integrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/operations-overview-cycle-1
      - uses: pnpm/action-setup@v4
        with:
          version: 10.15.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: platform/pnpm-lock.yaml
      - name: Patch main
        run: |
          python - <<'PY'
          from pathlib import Path
          p = Path('platform/apps/admin-web/src/main.tsx')
          s = p.read_text()
          if 'import { OperationsPanel } from "./OperationsPanel";' not in s:
              s = s.replace('import { InventoryPanel } from "./InventoryPanel";\n', 'import { InventoryPanel } from "./InventoryPanel";\nimport { OperationsPanel } from "./OperationsPanel";\n')
          marker = '''        {context?.permissions.includes("audit.read") ? (\n'''
          block = '''        {context?.permissions.includes("operations.read") ? (\n          <OperationsPanel supabase={supabase} />\n        ) : null}\n\n'''
          if block not in s:
              s = s.replace(marker, block + marker)
          p.write_text(s)
          PY
      - name: Format
        working-directory: platform
        run: |
          pnpm install --frozen-lockfile
          pnpm exec prettier --write apps/admin-web/src/main.tsx apps/admin-web/src/OperationsPanel.tsx
      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add platform/apps/admin-web/src/main.tsx platform/apps/admin-web/src/OperationsPanel.tsx
          git diff --cached --quiet || git commit -m "feat: integra visão operacional ao painel administrativo"
          git push origin HEAD:feature/operations-overview-cycle-1
