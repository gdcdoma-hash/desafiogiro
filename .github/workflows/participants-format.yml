name: Format participants panel

on:
  push:
    branches: [feature/participants-edit-cycle-2]
    paths: [.github/workflows/participants-format.yml]

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/participants-edit-cycle-2
      - uses: pnpm/action-setup@v4
        with:
          version: 10.15.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: platform/pnpm-lock.yaml
      - name: Format file
        working-directory: platform
        run: |
          pnpm install --frozen-lockfile
          pnpm exec prettier --write apps/admin-web/src/ParticipantsPanel.tsx
      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add platform/apps/admin-web/src/ParticipantsPanel.tsx
          git diff --cached --quiet || git commit -m "style: formata painel de participantes"
          git push origin HEAD:feature/participants-edit-cycle-2
