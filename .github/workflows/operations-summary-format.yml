name: Format operations summary

on:
  push:
    branches: [feature/operations-summary-cycle-2]
    paths: [.github/workflows/operations-summary-format.yml]

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/operations-summary-cycle-2
      - uses: pnpm/action-setup@v4
        with:
          version: 10.15.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: platform/pnpm-lock.yaml
      - name: Format files
        working-directory: platform
        run: |
          pnpm install --frozen-lockfile
          pnpm exec prettier --write apps/admin-web/src/OperationsPanel.tsx apps/admin-web/src/operations-summary.css
      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add platform/apps/admin-web/src/OperationsPanel.tsx platform/apps/admin-web/src/operations-summary.css
          git diff --cached --quiet || git commit -m "style: formata resumo operacional"
          git push origin HEAD:feature/operations-summary-cycle-2
