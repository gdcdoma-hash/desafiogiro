name: Format registrations UX

on:
  push:
    branches: [feature/registrations-ux-cycle-3]
    paths: [.github/workflows/registrations-ux-format.yml]

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/registrations-ux-cycle-3
      - uses: pnpm/action-setup@v4
        with:
          version: 10.15.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: platform/pnpm-lock.yaml
      - name: Format files
        working-directory: platform
        run: |
          pnpm install --frozen-lockfile
          pnpm exec prettier --write apps/admin-web/src/RegistrationsPanel.tsx apps/admin-web/src/registrations-summary.css
      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add platform/apps/admin-web/src/RegistrationsPanel.tsx platform/apps/admin-web/src/registrations-summary.css
          git diff --cached --quiet || git commit -m "style: formata acompanhamento das inscrições"
          git push origin HEAD:feature/registrations-ux-cycle-3
